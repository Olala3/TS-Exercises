/**
 * START: Follow the instructions below.
 */

// Create a `Country` type alias for the `string` type.
// Change the type for each variable to use the `Country` type.

type Country = {
    name: string;
}

const country1: Country = {name: 'Nigeria'};

const country2: Country = {name: 'Italy'};

const country3: Country =  {name: 'China'};

// ----

export {};
